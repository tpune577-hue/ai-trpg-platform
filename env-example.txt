# Database Configuration
# Replace with your actual PostgreSQL connection string
DATABASE_URL="postgresql://postgres:password@localhost:5432/ai-trpg?schema=public"

# OpenAI API Key for AI Game Master
# Get this from https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# NextAuth Configuration
# You can generate a secret with `openssl rand -base64 32` or just use a random string for dev
NEXTAUTH_SECRET="your-development-secret-key-change-in-prod"
NEXTAUTH_URL="http://localhost:3000"

# Public API URL
NEXT_PUBLIC_API_URL="http://localhost:3000"

# Node Environment
# Use 'development' for local dev, 'production' for deployment
NODE_ENV="development"
